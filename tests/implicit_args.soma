// expect: success
// Test: implicit argument syntax

def id : ∀ a. a -> a
    | x => x

def apply : ∀ a b. (a -> b) -> a -> b
    | f, x => f x

def test_id : Int32 := id 42
def test_apply : Int32 := apply id 100
