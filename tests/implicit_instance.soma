// expect: success
// Test: implicit instance arguments (type class style)
// classs are elaborated into ClassInfo with record types.
// Instance arguments ({{Display a}}) are resolved via the InstanceEnv.

class Display (a : Type) where
    display : a -> String

def show (a : Type) : {{Display a}} -> a -> String
    | x => display x
