// expect: success
// Test: unrestricted (omega) quantity annotation - use any number of times

def use_many : (ω a : Type) -> a -> a
    | _, x  => x

def duplicate : (ω x : Int32) -> (Int32, Int32)
    | x => (x, x)
