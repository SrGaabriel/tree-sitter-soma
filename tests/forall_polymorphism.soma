// expect: success
// Test:  polymorphism (implicit type arguments)

def id : ∀ a. a -> a
    | x => x

def const : ∀ a b. a -> b -> a
    | x, _ => x

def test1 : Int32 := id 42
def test2 : String := id "hello"
def test3 : Int32 := const 1 "ignored"
