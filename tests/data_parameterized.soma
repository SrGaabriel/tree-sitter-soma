// expect: success
// Test: parameterized algebraic data type

inductive Maybe {a : Type} where
    | Nothing
    | Just (value : a)

def from_maybe (a : Type) : a -> Maybe a -> a
    | default, Maybe::Nothing => default
    | _, (Maybe::Just x) => x
